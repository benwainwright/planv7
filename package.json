{
  "name": "planv7",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "scripts": {
    "test": "yarn workspaces foreach --interlaced --parallel run test"
  }
}
