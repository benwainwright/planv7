{
  "name": "planv7",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@planv7/jest-common": "workspace:packages/jest-common",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "test": "yarn workspaces foreach --interlaced --parallel run test",
    "lint": "yarn workspaces foreach --interlaced --parallel run lint"
  }
}
