{
  "name": "planv7",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@planv7/jest-common": "workspace:packages/jest-common",
    "jest": "^25.0.0",
    "ts-jest": "^25.5.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "start:dev": "webpack --env=dev",
    "test": "jest",
    "lint": "yarn workspace @planv7/domain lint && yarn workspace @planv7/framework lint && yarn workspace @planv7/application lint && yarn workspace @planv7/frontend lint && yarn workspace @planv7/backend lint"
  },
  "config": {
    "mongoDbMemoryServer": {
      "downloadDir": "node_modules/.cache/mongodb-memory-server/mongodb-binaries"
    }
  }
}
